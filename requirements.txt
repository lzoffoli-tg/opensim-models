numpy>=1.24
pandas>=2.0

# OpenSim Python bindings, required to build/pose/export an OpensimUser.
# On Windows, install via a compatible Conda environment if this fails:
#   conda install -c opensim-org opensim
opensim

# Testing
pytest>=8
